services:
  proxy:
    image: nginx:latest
    ports:
      - "80:80"  # This is the only port the outside world sees
    volumes:
      - ./nginx.conf:/etc/nginx/nginx.conf:ro
    depends_on:
      - dashboard

  dashboard:
    build: .
    deploy:
      replicas: 2
    # NO PORTS SECTION HERE!
